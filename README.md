# discord-scoring-bot

## Build

```bash
$ docker compose build
# OR
$ docker compose up -d --build
```

## Run

1. Register commands.

```bash
$ yarn register
```

2. Run the application.

```bash
$ docker compose up -d
```
